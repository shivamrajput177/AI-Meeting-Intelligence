# Go
/bin/

# Node / web
web/node_modules/
web/dist/
web/.vite/

# Local env overrides
.env
.env.local

# Real service configs — copied from configs/*.template.json and filled
# in with real values, so only the templates are checked in.
configs/*.json
!configs/*.template.json

# Editor/OS
.DS_Store
.idea/
.vscode/
